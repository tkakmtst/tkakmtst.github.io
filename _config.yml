# jekyll configuration
title: tkakmtst
domain: tkakmtst.github.io # if you want to force HTTPS, specify the domain without the http at the start, e.g. example.com
url: https://tkakmtst.github.io # the base hostname and protocol for your site, e.g. http://example.com
remote_theme: pages-themes/merlot@v0.2.0
plugins:
  - jekyll-remote-theme # add this line to the plugins list if you already have one
